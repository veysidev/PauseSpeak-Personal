const testButton = document.getElementById("testButton");
const statusText = document.getElementById("status");

testButton.addEventListener("click", () => {
  statusText.textContent = "🎉 PauseSpeak çalışıyor!";
});